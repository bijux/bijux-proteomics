# Contracts

Package contracts:

- runtime state transitions are explicit and validated
- artifact outputs are reproducible for equivalent execution inputs
- CLI and API surfaces align with shared runtime model contracts
- provider capability checks remain explicit and auditable

Behavioral changes in runtime execution must be reflected by tests and artifact
expectations.
