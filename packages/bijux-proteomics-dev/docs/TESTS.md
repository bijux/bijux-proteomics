# Tests

The package uses pytest tests located in `tests/`.

Each maintenance domain should include direct unit tests for parser and policy
logic, plus command-level behavior tests for CI reliability.
