# Local Package Archetypes

This directory is reserved for repository-local package archetypes.

If a package archetype is shared across Bijux Python repositories, include the
matching `makes/bijux-py/*.mk` module directly from the package profile instead
of adding a pass-through wrapper here.
