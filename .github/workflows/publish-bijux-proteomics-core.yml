name: publish-bijux-proteomics-core

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build:
    uses: ./.github/workflows/build-release-artifacts.yml
    with:
      package_slug: bijux-proteomics-core
      cache_dependency_path: |
        pyproject.toml
        packages/bijux-proteomics-core/pyproject.toml
      build_targets: "build-bijux-proteomics-core"
      package_dir: "packages/bijux-proteomics-core"
      dist_subdir: "build/bijux-proteomics-core"
      upload_paths: |
        artifacts/build/bijux-proteomics-core/*

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download publish artifacts
        uses: actions/download-artifact@v5
        with:
          name: bijux-proteomics-core-pypi-dist
          path: .github/tmp/bijux-proteomics-core-dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: .github/tmp/bijux-proteomics-core-dist
          skip-existing: true
