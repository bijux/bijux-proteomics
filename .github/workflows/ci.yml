# SSOT NOTICE: This file is generated by bijux-std standards sync.
# Source of truth: bijux-std/.github/standards/repo-config.manifest.json and bijux-std/shared/bijux-gh/workflows/.
# Do not edit this generated copy directly.

name: "continuous integration"
on:
  workflow_call:
    inputs:
      package_slug:
        required: true
        type: string
      package_dir:
        required: true
        type: string
      artifacts_dir:
        required: true
        type: string
      test_python_versions:
        required: false
        type: string
        default: "[\"3.11\"]"
      check_targets:
        required: false
        type: string
        default: "[\"quality\", \"security\", \"docs\", \"api\", \"build\", \"sbom\"]"
      api_toolchain_targets:
        required: false
        type: string
        default: "[\"api\"]"
      post_test_command:
        required: false
        type: string
        default: ""
permissions:
  contents: read
concurrency:
  group: "ci-${{ inputs.package_slug }}-${{ github.ref }}"
  cancel-in-progress: true
jobs:
  tests:
    name: "tests-${{ inputs.package_slug }}-py${{ matrix.python-version }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: "${{ fromJson(inputs.test_python_versions) }}"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "${{ matrix.python-version }}"
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock
      - name: "Install package toolchain"
        shell: bash
        run: "set -euo pipefail; makefile=\"${GITHUB_WORKSPACE}/makes/packages/$(basename '${{ inputs.package_dir }}').mk\"; make -f \"$makefile\" -C \"${{ inputs.package_dir }}\" install "
      - name: "Run tests"
        shell: bash
        run: "set -euo pipefail; makefile=\"${GITHUB_WORKSPACE}/makes/packages/$(basename '${{ inputs.package_dir }}').mk\"; make -f \"$makefile\" -C \"${{ inputs.package_dir }}\" test "
      - name: "Run post-test validation"
        if: "${{ inputs.post_test_command != '' }}"
        shell: bash
        env:
          ARTIFACTS_DIR: "${{ inputs.artifacts_dir }}"
        run: "${{ inputs.post_test_command }}"
      - name: "Upload test artifacts"
        if: "${{ always() }}"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: "${{ inputs.package_slug }}-test-py${{ matrix.python-version }}"
          path: "${{ inputs.artifacts_dir }}/test"
          if-no-files-found: ignore
          retention-days: 14
    if: "${{ github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]' }}"
  checks:
    name: "checks-${{ inputs.package_slug }}-${{ matrix.target }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: "${{ fromJson(inputs.check_targets) }}"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: 3.11
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        if: "${{ contains(fromJson(inputs.api_toolchain_targets), matrix.target) }}"
        with:
          node-version: 20
      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961
        if: "${{ contains(fromJson(inputs.api_toolchain_targets), matrix.target) }}"
        with:
          distribution: temurin
          java-version: 17
      - name: "Install package toolchain"
        shell: bash
        run: "set -euo pipefail; makefile=\"${GITHUB_WORKSPACE}/makes/packages/$(basename '${{ inputs.package_dir }}').mk\"; make -f \"$makefile\" -C \"${{ inputs.package_dir }}\" install "
      - name: "Run ${{ matrix.target }}"
        shell: bash
        run: "set -euo pipefail; makefile=\"${GITHUB_WORKSPACE}/makes/packages/$(basename '${{ inputs.package_dir }}').mk\"; make -f \"$makefile\" -C \"${{ inputs.package_dir }}\" ${{ matrix.target }} "
      - name: "Upload artifacts"
        if: "${{ always() && hashFiles(format('{0}/{1}/**', inputs.artifacts_dir, matrix.target)) != '' }}"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: "${{ inputs.package_slug }}-${{ matrix.target }}"
          path: "${{ inputs.artifacts_dir }}/${{ matrix.target }}"
          if-no-files-found: ignore
          retention-days: 14
    if: "${{ github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]' }}"
  lint:
    name: "lint-${{ inputs.package_slug }}"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: 3.11
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock
      - name: "Install package toolchain"
        shell: bash
        run: "set -euo pipefail; makefile=\"${GITHUB_WORKSPACE}/makes/packages/$(basename '${{ inputs.package_dir }}').mk\"; make -f \"$makefile\" -C \"${{ inputs.package_dir }}\" install "
      - name: "Run lint"
        shell: bash
        run: "set -euo pipefail; makefile=\"${GITHUB_WORKSPACE}/makes/packages/$(basename '${{ inputs.package_dir }}').mk\"; make -f \"$makefile\" -C \"${{ inputs.package_dir }}\" lint "
      - name: "Upload lint artifacts"
        if: "${{ always() }}"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: "${{ inputs.package_slug }}-lint"
          path: "${{ inputs.artifacts_dir }}/lint"
          if-no-files-found: ignore
          retention-days: 14
    if: "${{ github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]' }}"
