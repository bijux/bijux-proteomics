# SSOT NOTICE: This file is generated by bijux-std standards sync.
# Source of truth: bijux-std/.github/standards/repo-config.manifest.json and bijux-std/shared/bijux-gh/workflows/.
# Do not edit this generated copy directly.

name: "repo / verify"
on:
  push:
    branches:
      - main
    paths:
      - ".bijux/**"
      - ".github/**"
      - "apis/**"
      - "configs/**"
      - "docs/**"
      - "makes/**"
      - "packages/**"
      - Makefile
      - mkdocs.shared.yml
      - mkdocs.yml
      - pyproject.toml
      - tox.ini
      - uv.lock
  pull_request:
    branches:
      - main
    paths:
      - ".bijux/**"
      - ".github/**"
      - "apis/**"
      - "configs/**"
      - "docs/**"
      - "makes/**"
      - "packages/**"
      - Makefile
      - mkdocs.shared.yml
      - mkdocs.yml
      - pyproject.toml
      - tox.ini
      - uv.lock
  workflow_dispatch:
  merge_group:
    branches:
      - main
permissions:
  contents: read
  actions: read
  pull-requests: read
concurrency:
  group: "verify-${{ github.ref }}"
  cancel-in-progress: true
jobs:
  policy_gate:
    name: policy-prerequisites
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: "Wait for policy and standards prerequisites"
        shell: bash
        env:
          GITHUB_TOKEN: "${{ github.token }}"
        run:
          |
            set -euo pipefail
            python3 .github/scripts/check_workflow_prerequisites.py
  repository:
    name: repository-contracts
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: 3.11
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock
      - name: "Verify repository automation contracts"
        shell: bash
        run: "set -euo pipefail; make check-shared-bijux-py check-config-layout check-make-layout help "
    needs: policy_gate
  package:
    name: "${{ matrix.package_slug }}"
    needs:
      - policy_gate
      - repository
    strategy:
      fail-fast: false
      matrix:
        include:
          - package_slug: agentic-proteins
            package_dir: packages/agentic-proteins
            artifacts_dir: artifacts/agentic-proteins
          - package_slug: bijux-proteomics-foundation
            package_dir: packages/bijux-proteomics-foundation
            artifacts_dir: artifacts/bijux-proteomics-foundation
          - package_slug: bijux-proteomics-core
            package_dir: packages/bijux-proteomics-core
            artifacts_dir: artifacts/bijux-proteomics-core
          - package_slug: bijux-proteomics-intelligence
            package_dir: packages/bijux-proteomics-intelligence
            artifacts_dir: artifacts/bijux-proteomics-intelligence
          - package_slug: bijux-proteomics-knowledge
            package_dir: packages/bijux-proteomics-knowledge
            artifacts_dir: artifacts/bijux-proteomics-knowledge
          - package_slug: bijux-proteomics-lab
            package_dir: packages/bijux-proteomics-lab
            artifacts_dir: artifacts/bijux-proteomics-lab
          - package_slug: bijux-proteomics-dev
            package_dir: packages/bijux-proteomics-dev
            artifacts_dir: artifacts/bijux-proteomics-dev
            check_targets: "[\"quality\", \"security\", \"build\", \"sbom\"]"
    uses: ./.github/workflows/ci.yml
    with:
      package_slug: "${{ matrix.package_slug }}"
      package_dir: "${{ matrix.package_dir }}"
      artifacts_dir: "${{ matrix.artifacts_dir }}"
      test_python_versions: "${{ matrix.test_python_versions || '[\"3.11\"]' }}"
      check_targets: "${{ matrix.check_targets || '[\"quality\", \"security\", \"docs\", \"api\", \"build\", \"sbom\"]' }}"
      api_toolchain_targets: "${{ matrix.api_toolchain_targets || '[\"api\"]' }}"
      post_test_command: "${{ matrix.post_test_command || '' }}"
  verification_ready:
    name: verification-ready
    if: "${{ always() }}"
    needs:
      - policy_gate
      - repository
      - package
    runs-on: ubuntu-latest
    steps:
      - name: "Require repository and package verification success"
        shell: bash
        env:
          REPOSITORY_RESULT: "${{ needs.repository.result }}"
          PACKAGE_RESULT: "${{ needs.package.result }}"
        run:
          |
            set -euo pipefail
            [[ "${REPOSITORY_RESULT}" == "success" ]]
            [[ "${PACKAGE_RESULT}" == "success" ]]
          
