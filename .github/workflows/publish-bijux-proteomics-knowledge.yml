name: publish-bijux-proteomics-knowledge

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build:
    uses: ./.github/workflows/build-release-artifacts.yml
    with:
      package_slug: bijux-proteomics-knowledge
      cache_dependency_path: |
        pyproject.toml
        packages/bijux-proteomics-knowledge/pyproject.toml
      build_targets: "build-bijux-proteomics-knowledge"
      package_dir: "packages/bijux-proteomics-knowledge"
      dist_subdir: "build/bijux-proteomics-knowledge"
      upload_paths: |
        artifacts/build/bijux-proteomics-knowledge/*

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download publish artifacts
        uses: actions/download-artifact@v5
        with:
          name: bijux-proteomics-knowledge-pypi-dist
          path: .github/tmp/bijux-proteomics-knowledge-dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: .github/tmp/bijux-proteomics-knowledge-dist
          skip-existing: true
