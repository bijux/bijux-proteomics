name: ci-bijux-proteomics-foundation

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/ci-bijux-proteomics-foundation.yml"
      - ".github/workflows/ci-package.yml"
      - "packages/bijux-proteomics-foundation/**"
      - "configs/**"
      - "makes/**"
      - "Makefile"
      - "pyproject.toml"
  pull_request:
    paths:
      - ".github/workflows/ci-bijux-proteomics-foundation.yml"
      - ".github/workflows/ci-package.yml"
      - "packages/bijux-proteomics-foundation/**"
      - "configs/**"
      - "makes/**"
      - "Makefile"
      - "pyproject.toml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/ci-package.yml
    with:
      package_slug: bijux-proteomics-foundation
      cache_dependency_path: uv.lock
      test_paths: packages/bijux-proteomics-foundation/tests
      lint_dirs: packages/bijux-proteomics-foundation/src/bijux_proteomics_foundation
      quality_paths: packages/bijux-proteomics-foundation/src/bijux_proteomics_foundation
      build_target: build-bijux-proteomics-foundation
