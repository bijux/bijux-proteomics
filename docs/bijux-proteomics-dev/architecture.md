# Architecture

The package is organized by maintenance domains:

- `quality` for lint and repository policy checks
- `security` for dependency and vulnerability gates
- `api` for OpenAPI drift checks
- `release` for version and changelog checks
- `docs` for documentation consistency checks
- `tools` for operator utility commands
